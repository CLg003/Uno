
const Player1Cards = ({player1Cards}) => {

    return (
        <p>cards should be here</p>
    );

    // player1Cards.map((card) => {
    //     return (
    //         <div>
    //             <img src={`${process.env.PUBLIC_URL}${card.image}`}/>
    //             <p>cards should be here</p>
    //         </div> 
            
    //     );
    // });
    
}

export default Player1Cards;